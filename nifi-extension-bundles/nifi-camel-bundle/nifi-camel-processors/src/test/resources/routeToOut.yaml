- from:
    uri: "direct:in"
    steps:
      - transform:
          expression:
            simple: "Received camel message: \"${body}\" and headers ${headers}"
      - convertBodyTo:
          type: java.io.InputStream
      - to:
          uri: "direct:out"